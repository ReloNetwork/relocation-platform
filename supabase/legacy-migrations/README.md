# Historical database SQL

These files describe retired systems or tables that were created before Supabase CLI migration history was established.

They are retained for audit and recovery reference only. Supabase CLI does not scan this directory, and these files must not be copied back into `supabase/migrations` without a schema review.

For the redesigned launch:

- Beehiiv replaces the legacy Resend Landing List sequence.
- The qualification-first client and partner journeys use the active launch tables.
- The old directory, consultation and AI talent flows are not part of the public launch schema.
